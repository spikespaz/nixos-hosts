# Claude Session Guidelines

Guidelines and skills are maintained on branch `claude/nix-architecture-v2`. Read them before starting work:

```bash
git show claude/nix-architecture-v2:CLAUDE.md
```

Start with the `session-continuity` skill — it contains the full boot sequence:

```bash
git show claude/nix-architecture-v2:.claude/skills/session-continuity/skill.md
```
